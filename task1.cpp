#include <iostream>

int main(){
    int a,b,c ;
    cin >> a >> b >> c;
    if (a + b > c) and (b + c > a) and (c + a > b);
        cout >> "the triangle exists";
    else
        cout >> "the triangle does not exist"
}